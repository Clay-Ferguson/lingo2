[project]
name = "lingo-voice-typer"
version = "0.1.0"
description = "GTK4 system-wide voice-to-text input app"
requires-python = ">=3.10"
dependencies = [
    "sounddevice>=0.4.6",
    "numpy>=1.21.0",
    "PyYAML>=6.0",
]

[tool.uv]
package = false

# Voice Typer GTK App Dependencies
#
# Install with: pip install -r requirements.txt
#
# Note: PyGObject (GTK4 bindings) requires system packages first:
#   Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0
#   Fedora: sudo dnf install python3-gobject gtk4
#   Arch: sudo pacman -S python-gobject gtk4

# Audio capture - provides direct PCM access to microphone
sounddevice>=0.4.6

# Numerical processing for RMS calculation
numpy>=1.21.0

# Keyboard simulation for typing transcribed text
pynput>=1.7.6
